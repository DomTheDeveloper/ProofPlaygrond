# OEIS A263135 Lean audit

This branch verifies immutable repaired commit `6ad03f2a0fa5de81c0ed08fd0fc38a42f12513b7` from `DomTheDeveloper/formal-conjectures`.

The mathematically necessary ordered-side hypothesis `a ≤ c` is committed directly into the clipping cardinality, geometry, contact, and terminal witness modules. The balanced extremal parameters supply `a ≤ b ≤ c`. The row-bound cancellation was also rewritten as an explicit doubled inequality discharged by `omega`, avoiding a version-sensitive API dependency.

The AXLE lane flattens the custom module graph into one `import Mathlib` development, omits the original open catalog declaration, checks the new terminal theorem, and records its axiom report. The separate pinned Lean 4.27 kernel lane rejects `sorry`, `admit`, `native_decide`, unsafe/custom axioms, compiler-trust shortcuts, and `sorryAx`.
