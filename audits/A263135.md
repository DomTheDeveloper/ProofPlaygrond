# OEIS A263135 Lean audit

This branch verifies immutable repaired commit `1a99472b0118d4fa9049e1e9dd528c93f0ede558` from `DomTheDeveloper/formal-conjectures`.

The mathematically necessary ordered-side hypothesis `a ≤ c` is now committed directly into the clipping cardinality, geometry, contact, and terminal witness modules. The balanced extremal parameters supply `a ≤ b ≤ c`.

The AXLE lane flattens the custom module graph into one `import Mathlib` development, omits the original open catalog declaration, checks the new terminal theorem, and records its axiom report. The separate pinned Lean 4.27 kernel lane rejects `sorry`, `admit`, `native_decide`, unsafe/custom axioms, compiler-trust shortcuts, and `sorryAx`.
