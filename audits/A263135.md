# OEIS A263135 repaired AXLE audit

Verify the exact honeycomb-lattice maximum-contact theorem using the statically audited, ordered-side-repaired proof at `DomTheDeveloper/formal-conjectures@6ad03f2a0fa5de81c0ed08fd0fc38a42f12513b7` in AXLE's Lean 4.27 environment.

The proof source commits `a ≤ c` throughout the clipping chain, derives `a ≤ b ≤ c` from the balanced parameters, and uses an explicit doubled row-bound inequality discharged by `omega`. The flattener alpha-renames named private helpers by source module, preserving native module privacy during single-file AXLE verification.
