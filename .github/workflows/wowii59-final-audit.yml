# Copyright 2026 Dominic Dabish
# Licensed under the Apache License, Version 2.0.
# Boolean-indicator core audit for source commit 7764705bb0cafd673bd8b830f2e57ca0b52d10c3.

name: WOWII 59 final audit

on:
  pull_request:
    branches: [main]
    paths:
      - '.github/workflows/wowii59-final-audit.yml'
      - 'audits/wowii59-final.md'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  audit:
    runs-on: ubuntu-latest
    timeout-minutes: 120
    steps:
      - name: Checkout exact reduced-certificate source
        uses: actions/checkout@v6
        with:
          repository: DomTheDeveloper/formal-conjectures
          ref: 7764705bb0cafd673bd8b830f2e57ca0b52d10c3
          fetch-depth: 1

      - name: Record exact commit
        run: git rev-parse HEAD | tee "$RUNNER_TEMP/resolved-sha.txt"

      - name: Install repository-pinned Lean
        run: |
          set -euo pipefail
          curl -fsSL https://raw.githubusercontent.com/leanprover/elan/master/elan-init.sh \
            | sh -s -- -y --default-toolchain none
          echo "$HOME/.elan/bin" >> "$GITHUB_PATH"

      - name: Restore Lean build cache
        uses: actions/cache@v4
        with:
          path: |
            .lake/packages
            .lake/build
          key: wow59-${{ runner.os }}-${{ hashFiles('lean-toolchain', 'lake-manifest.json', 'lakefile.toml') }}
          restore-keys: wow59-${{ runner.os }}-

      - name: Restore pinned Mathlib cache
        run: |
          set -euo pipefail
          "$HOME/.elan/bin/lake" exe cache unpack || true
          "$HOME/.elan/bin/lake" exe cache get

      - name: Build shared Formal Conjectures utilities
        run: |
          set -euo pipefail
          "$HOME/.elan/bin/lake" build FormalConjecturesUtil

      - name: Reject placeholders and trust shortcuts
        run: |
          set -euo pipefail
          target='Scratch/WOWII59CoreTest.lean'
          if grep -nE '\b(sorry|admit|native_decide|unsafe)\b|(^|[^A-Za-z])axiom([^A-Za-z]|$)|bv_decide|bv_check|Lean\.(trustCompiler|ofReduce|ofReduceBool)' "$target"; then
            echo 'Forbidden placeholder or trust shortcut found.' >&2
            exit 1
          fi

      - name: Compile reduced 11-vertex core certificate
        run: |
          set -o pipefail
          "$HOME/.elan/bin/lake" env lean Scratch/WOWII59CoreTest.lean \
            2>&1 | tee "$RUNNER_TEMP/wowii59-final.log"

      - name: Enforce axiom policy
        run: |
          set -euo pipefail
          if grep -E 'sorryAx|_native\.bv_decide|Lean\.(trustCompiler|ofReduce|ofReduceBool)' "$RUNNER_TEMP/wowii59-final.log"; then
            echo 'Forbidden axiom or compiler-trust dependency detected.' >&2
            exit 1
          fi

      - name: Upload transcript
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: wowii59-core-certificate-${{ github.run_id }}
          path: |
            ${{ runner.temp }}/resolved-sha.txt
            ${{ runner.temp }}/wowii59-final.log
          if-no-files-found: warn
